{
  "name": "scripts",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "schema-generate": "npx tsc --outDir dist && node dist/main.js --generate",
    "parse": "npx tsc --outDir dist && node dist/main.js",
    "generate": "prisma generate --schema ./data/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "3.14.0",
    "axios": "^0.27.2",
    "prisma": "^3.14.0",
    "typescript": "^4.6.3"
  }
}
